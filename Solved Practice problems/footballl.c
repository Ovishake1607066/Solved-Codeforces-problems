#include<stdio.h>
#include<string.h>
int main()
{
    int i,j,b,k=0,s=0,a[100];
    for(i=1; i++)(scanf("%d",&a[i])==1 && a[i]==0 && a[i]==1)
    {
        if(scanf("%d",&a[i])==1 && a[i]==0 && a[i]==1)
        {
            continue;
        }
        else if(scanf("%d",&a[i])==1 && a[i]==0 && a[i]==1)
    }
    printf("%d\n",b);

        for(j=1;j<=(b-6);j++)
        {
            for(i=j;i<=7;i++)
            {
                s=a[i]+48+s;
                if(s==0 || s==7)
                {
                    k=1;
                }
            }
        }
        if(k==1)
        {
            printf("YES\n");
        }
        else
        {
            printf("NO\n");
        }
    }
